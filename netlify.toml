[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# SPA fallback (keep this AFTER the api rule)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200